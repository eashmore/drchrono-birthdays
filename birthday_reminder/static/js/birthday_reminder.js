function attachSendAllEvent(user_id) {
  var button = document.getElementById('email-all');
  button.addEventListener('click', sendAllPatientsEmails(user_id));
}

function sendAllPatientsEmails(user_id) {

}
